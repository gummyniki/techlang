!import(std.tec) as std;

struct person = {
    int age;
    float height;
}

function main() returns none {
    person p;
    p.age = 30;
    p.height = 1.75;
    std.print(p.age);
    std.print(p.height);

    for (int i = 0, i < 10, i += 1) do {
	std.print(i);
    }
}
